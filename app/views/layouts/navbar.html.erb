<nav class="navbar navbar-inverse">
    <a class="navbar-brand" href="/" data-method="GET">Home</a>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
            <li class="active"><a href="/">Accueil <span class="sr-only">(current)</span></a></li>
            <% if user_signed_in? && current_user.company %>
            <li><a href="<%= card_path %>">Liste des etudiants</a></li>
            <% end %>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    <%= user_signed_in? ? current_user.username : "Liens" %>
                    <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <% if !user_signed_in? %>
                        <li><a href="<%= new_user_registration_path %>" data-method="get">S'inscrire</a></li>
                        <li role="separator" class="divider"></li>
                        <li><a href="<%= new_user_session_path %>" data-method="get">Se connecter</a></li>
                        <% else %>


                        <% if current_user.status == "admin" %>
                        <li><a href="/admin" data-method="get">Dashboard</a></li>
                        <% else %>
                        <li><a href="<%= mondashboard_path %>" data-method="get">Mon dashboard</a></li>
                        <li role="separator" class="divider"></li>
                        <li>
                            <a href="<%= current_user.company ? company_path(Company.find_by(user_id: current_user.id).id) : "/monprofil/#{(Student.find_by(user_id: current_user.id).id)}" %>" data-method="get">Mon profil</a>
                        </li>
                        <li style="<%= current_user.company ? 'display: none;' : '' %>">
                            <a href="<%= current_user.company ? company_path(Company.find_by(user_id: current_user.id).id) : "/moncv/#{current_user.student.cv.id}" %>" data-method="get">Mon cv</a>
                        </li>
                        <% end %>
                        <li role="separator" class="divider"></li>
                        <li><a href="<%= destroy_user_session_path %>" data-method="delete">Se deconnecter</a></li>
                        <% end %>
                    </ul>
                </li>

            </ul>
            <% if user_signed_in? && current_user.student %>
            <%= render template: 'students/navbar' %>
            <% end %>
        </div>
    </nav>
